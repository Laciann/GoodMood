extends layout


include mixins/_pagination
include mixins/_storeCard

block content
  .inner
    h2= title
   
    .stores
      each store, index in stores 
         +storeCard(store, index)
         
         
  
    +pagination(page, pages, count)
